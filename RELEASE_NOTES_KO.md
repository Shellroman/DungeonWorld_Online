# Dungeon World Online v1.0.0 · 공개 출시

Dungeon World Online의 **첫 공개 배포판**입니다. 개발 과정에서 사용하던 실험/내부 릴리스 표기를 제거하고, 공개 배포용 버전 체계를 **v1.0.0**부터 시작합니다.

## 핵심

- English를 기본 언어로 사용하는 한국어 / English 이중 언어 UI와 기본 데이터
- Windows x64 단일 실행 런처 + 브라우저 플레이 화면
- 활성 캠페인만 참가할 수 있는 서버 측 접속 검증
- 캠페인 생성 시 GM 제외 2~8명 플레이어 정원 선택 · 기본 4명 · 서버 측 정원 강제
- 캐릭터, 행동, 주문, 종족 특성, 인벤토리, NPC, 몬스터, 세션/로그 관리
- 행동 효과와 타직업 획득을 위한 설명형 편집/선택 UI
- 미분류 주문의 일반 습득 차단
- 별도 `탄약 / Ammo` 인벤토리 자원과 구버전 데이터 이관
- 로컬 파일 기반 BGM/효과음
- DWPack 가져오기/내보내기 및 한·영 기본 팩
- 도움말, 동적 UI, 툴팁까지 포함한 한·영 번역 정리
- 폰트 의존 `ⓘ` 대신 안정적인 CSS 기반 정보 아이콘
- 전체 UI의 버튼/폼/모달 간격과 가시성 보강

## 최종 영문 모드 수정

- English 모드의 동적 문장 안에 기본 직업·종족·행동·주문·몬스터 폴더명이 한국어로 남던 문제를 수정했습니다.
- 최초 캐릭터 운명 선택의 `Class / Spells` 제목과 확정 대화상자를 포함해 삭제 확인, 다중직업, 몬스터 태그, XP, 주문 레벨 검증, 툴팁, 서버 오류 문구를 재점검했습니다.
- 동적으로 바뀌는 `title` / `placeholder` / `aria-label` / `data-tip` 속성도 English 모드에서 다시 번역하도록 보강했습니다.
- 네이티브 확인창의 줄바꿈이 `\n` 문자 그대로 보이던 경우도 함께 수정했습니다.
- 사용자가 직접 만든 한국어 고유명은 English 모드에서도 임의 번역하지 않고 그대로 유지합니다.

## 최종 규칙·플레이 수정

- 최대 하중이 근력 원점수가 아니라 **STR 수정치**를 사용하도록 바로잡고, 서버 인벤토리 요약에서도 동전 무게를 동일하게 반영합니다.
- 피해 굴림은 최종값이 0 아래로 내려가지 않도록 수정하되, 일반 판정의 음수 결과는 그대로 유지합니다.
- 몬스터의 **피해 고굴림**이 실제로 두 번 굴려 높은 결과를 사용하도록 수정하고, 주사위 프리셋 전환 시 이전 수정치/고굴림 상태가 남지 않게 했습니다.
- 빠른 몬스터 제작의 영구 마법 방호는 장갑 4와 함께 `마법적` 태그를 자동 적용합니다.
- 주문 운용을 직업명 하드코딩 대신 데이터 기반 프로필로 전환했습니다. 주문 습득·준비·한도·0레벨 자동 처리·시작/레벨업 습득·표시 명칭을 직업 데이터로 설정합니다.
- 타직업 주문 체계 접근은 원본 직업의 프로필을 상속하며, 다른 직업 행동을 얻은 뒤 그 행동이 다시 제3의 주문 체계를 부여하는 중첩 접근도 처리합니다.
- 몬스터 도감 공개 항목의 English 번역을 완성된 문장 단위로 정리하고, GM이 확인 후 모든 정보를 즉시 공개·저장하는 **전부 공개 / Reveal All** 기능을 추가했습니다.

## 최종 무결성 감사 및 기본 데이터 보강

- 기본 몬스터 데이터를 **154개**로 보강했으며, `이계의 존재`도 14개를 포함합니다. 한국어/English Core DWPack에 동일한 목록을 제공합니다.
- 몬스터 목록은 **폴더만 드래그 정렬**할 수 있고, 몬스터 자체는 드래그 이동되지 않습니다. 몬스터 이동은 편집기의 `지역 / 폴더`에서 명시적으로 수행합니다.
- 고급행동 포인트는 일반 고급행동·다중직업·확장직업 전설행동/직업행동이 하나의 누적 포인트 풀을 공유하며, 사용하지 않은 포인트는 이후 레벨에도 유지됩니다.
- `플레이어가 다중직업 행동 직접 선택` 규칙이 UI뿐 아니라 서버에서도 실제로 적용되도록 수정했습니다.
- **능력치 성장 포인트**를 서버에서 검증하고, 캠페인의 능력치 최소/최대 설정을 실제 캐릭터 편집 규칙으로 적용했습니다. 현재 캐릭터를 범위 밖으로 만드는 규칙 변경은 거부됩니다.
- 직업 피해 주사위와 캐릭터 피해 주사위를 실제 주사위 엔진이 지원하는 `D4/D6/D8/D10/D12`로 일치시켜, 저장은 되지만 굴릴 수 없던 잘못된 주사위 값을 차단·정규화합니다.
- BGM의 `끝나면 다음 곡`은 마지막 곡에서 정지하고, `재생목록 반복`만 처음 곡으로 돌아가도록 구분했습니다.
- 확장직업 최대 개수는 0~99로 정리하고, 거절했던 확장직업을 다시 제안해 상한을 우회하던 경우 및 현재 보유 수 아래로 상한을 낮추는 경우를 차단했습니다.
- 직업/종족 이름 변경 시 타직업 주문 출처·반대 종족 특성 참조를 함께 갱신하며, 직업 행동 삭제/변경 후 보이지 않는 옛 선택이 포인트를 계속 소비하지 않도록 상태를 정리합니다.
- English 모드에서 규칙 화면만 저장했을 때 기본 재화 `닢`이 의도치 않게 데이터 자체의 `Coin`으로 바뀌던 문제를 수정했습니다.


## 최종 규칙 저장 / English 몬스터 핫픽스

- GM 규칙 저장 시 발생하던 `r is not defined` 오류를 수정했습니다. 저장 핸들러가 현재 캠페인 규칙 객체를 먼저 참조한 뒤 변경되지 않은 값도 안전하게 보존합니다.
- English UI/콘텐츠 번역 JSON을 현재 BUILD_ID로 캐시 버전 관리하고 `no-store`로 불러오도록 변경해, 새 JavaScript/몬스터 데이터와 예전 브라우저 캐시 번역표가 섞이던 문제를 차단했습니다.
- 기본 **154개 몬스터 전부**에 명시적인 English 표시 데이터를 넣고, English Dungeon World SRD의 몬스터 지역명/몬스터 이름과 다시 대조했습니다.
- `Guardsman`, `Fool`, `Hunter` 등 일부 기존 영문명을 원문 표기에 맞추고 Goblin Orkaster 및 English Core DWPack도 재검증했습니다.
- `Create Monster`, 몬스터 편집 필드, 굴림 방식 설명, 태그, 본능, 특기, 행동, 설명, 도감 공개 UI가 모두 같은 빌드의 최신 English 번역표를 사용합니다.

## 최종 사용자 제작 데이터 / 번역 출처 분리

- UI 번역표와 Dungeon World 기본 콘텐츠 번역표를 분리했습니다. GM/플레이어가 직접 만든 직업·종족·행동·주문·몬스터·확장직업 데이터는 기본 데이터와 같은 문자열을 사용해도 자동 번역하지 않습니다.
- 예를 들어 사용자가 만든 확장직업의 자원 이름이 `암흑`이어도 English 모드에서 기본 사제 주문 `Darkness`로 바뀌지 않고 작성한 `암흑` 그대로 유지됩니다.
- 기본 제공 데이터는 `_builtin` / `builtin` 출처 정보가 확인되는 경우에만 English 콘텐츠 번역을 사용합니다. 사용자 제작 데이터와 기본 데이터가 같은 이름을 갖는 경우에는 사용자 작성값 보존을 우선합니다.
- 확장직업의 한국어 상태 표기는 요청한 용어인 `히든`으로 유지하고 English에서는 `Hidden`으로 표시합니다.
- 타직업 주문 준비 경로에서 발생하던 `available is not defined` JavaScript 오류를 수정했습니다.
- 몬스터 규칙 태그도 화면용 영문 표시와 저장용 원본값을 분리해, English 모드에서 규칙을 저장하는 것만으로 기본/사용자 태그가 의도치 않게 번역된 값으로 덮어써지지 않습니다.

## 공개 배포 정리

- 화면과 파일명에서 개발 과정의 내부 브랜딩을 제거하고 공개 배포 명칭을 통일했습니다.
- 프로그램 코드 저작권자를 **ShellRoman**으로 명확히 하고 루트 `LICENSE`에 MIT License를 배치했습니다.
- Dungeon World 원작, 한국어 공개판, SRD 5.1 상위 고지, Unlimited Dungeons / Distant Shore 자료를 `THIRD_PARTY_NOTICES.md`에 통합했습니다.
- 한국어 Core DWPack manifest에도 **김성일 / 도서출판 초여명** 번역 attribution을 포함합니다.
- 생성형 AI 사용 범위를 `AI_USE_NOTICE.md`에 투명하게 공개합니다.
- 앱 내 도움말의 라이선스/출처 영역도 같은 구조로 정리했습니다.
- 공개 런처의 사용자 데이터 경로를 `DungeonWorldOnline`으로 정리하고, 이전 개발판 데이터 폴더는 한 번 자동 이관하거나 안전하게 폴백합니다.

## 호환성

- 앱 버전: **1.0.0**
- DB schema: **20**
- DWPack format: **1**
- BUILD_ID: **`bbf02338d0f83dc9`**

개발판에서 생성된 기존 schema 13~19 캠페인의 순차 migration 경로를 유지합니다. schema 20에서는 플레이어 정원을 추가하고 기존 캠페인의 현재 활성 참가자 수를 고려해 안전하게 이관합니다.

## Final UI polish

- 주사위 도형별 안쪽 여백과 테두리를 미세 조정해 d4/d6/d8/d10/d12의 외곽선이 더 균일하게 보이도록 마무리했습니다.
